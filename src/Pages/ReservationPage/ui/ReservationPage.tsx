import { Layout } from "Widgets";
import { FC } from "react";

const ReservationPage: FC = () => {
    return (
        <Layout>
            Ни один клиент пока не записался
        </Layout>
    )
}

export default ReservationPage
